import { colors } from "@vaija/shared";

export const theme = {
  colors: {
    ...colors,
    background: colors.white,
    card: colors.gray,
  },
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
  },
  radius: {
    sm: 8,
    md: 12,
    lg: 16,
    xl: 24,
  },
};
